steps:
  - name: "Build Docker Image"
    command: |
      sudo docker build -t pmagas/docker-react -f Dockerfile.dev .

  - name: "Run Docker Container"
    command: |
      sudo docker run -e CI=true pmagas/docker-react npm run test-cov
